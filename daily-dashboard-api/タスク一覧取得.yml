info:
  name: タスク一覧取得
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/tasks
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
