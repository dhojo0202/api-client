info:
  name: タスク追加
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/tasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
