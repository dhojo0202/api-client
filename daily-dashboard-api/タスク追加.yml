info:
  name: タスク追加
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/tasks
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
      disabled: true
  body:
    type: json
    data: |-
      {
        "title": "Brunoからのタスク001",
        "dueDate": "2026-04-30"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
